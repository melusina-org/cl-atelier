---
description: Format call with long control string broken via concatenate
inspector: check-line-length
comparison: string
---
(format stream "Processing ~A entries from database ~A with configuration profile ~A and output mode ~A enabled" count db-name profile mode)
---
(format stream
  #.(concatenate 'string
      "Processing ~A entries from database ~A "
      "with configuration profile ~A "
      "and output mode ~A enabled")
  count db-name profile mode)
---
